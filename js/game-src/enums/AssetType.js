/**
 * Enum for types of assets that can be loaded.
 * @enum {number}
 */
const AssetType = {
    /** GLTF file (.glb, .gltf). */
    GLTF: 0,
    /** Audio file. */
    AUDIO: 1
}

export default AssetType;